# Coupling Exempt

列出不需要镜像 doc 的 src 文件相对路径。每行一条。

`domains/deep_learning/audit_coupling.py` 跳过这些路径的 `uncovered-artifact` 检查。

---

# 默认豁免规则(脚本内置,无需在此列出):
# - 所有 __init__.py
# - 所有以 _ 开头的文件

# 项目特定豁免(示例):
# src/legacy/old_module.py
# src/utils/io_helpers.py
