interface:
  display_name: "Agentic Structural Harness"
  short_description: "Light semantic workspace for human-agent projects"
  default_prompt: "Use this harness to give a project .agentic control, semantic memory, and workspace execution layers; create mirror docs and event records; keep audit light; leave judgment to the human-agent loop."
